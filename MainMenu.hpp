/** @file MainMenu.hpp
 * @brief Заголовочный файл для класса MainMenu, управляющего главным меню игры.
 * @author akonch
 */

#ifndef MAINMENU_HORIZON /**< Проверяет, не был ли файл уже включён, чтобы избежать повторного включения. */
#define MAINMENU_HORIZON /**< Определяет макрос MAINMENU_HORIZON для защиты от повторного включения. */

#include <vector> /**< Подключает библиотеку vector для хранения пунктов меню. */
#include <string> /**< Подключает библиотеку string для работы с текстом пунктов меню. */

/** @brief Класс MainMenu, отвечающий за отображение и обработку главного меню: отрисовка пунктов, обработка выбора и настройка сложности.
 * @details Использует библиотеку ncurses для консольного интерфейса.
 * @author akonch
 */
class MainMenu { /**< Объявляет класс MainMenu. */
public: /**< Начинает секцию публичных методов и членов класса. */
    MainMenu(); /**< Конструктор класса MainMenu, инициализирует пункты меню. */
    int display(); /**< Отображает меню и возвращает выбранный пункт (0: начать, 1: сложность, 2: выход). */
    int selectDifficulty(); /**< Отображает подменю выбора сложности и возвращает выбранную сложность (0: лёгкая, 1: средняя, 2: сложная). */

private: /**< Начинает секцию приватных членов класса. */
    std::vector<std::string> menuItems; /**< Вектор строк с названиями пунктов главного меню. */
    std::vector<std::string> difficultyItems; /**< Вектор строк с названиями уровней сложности. */
    void renderMenu(const std::vector<std::string>& items, int selected); /**< Рендерит меню с указанными пунктами и выделенным элементом. */
}; /**< Завершает определение класса MainMenu. */

#endif /**< Завершает защиту от повторного включения файла. */
#endif